Bootstrap: docker
From: ubuntu:22.04
Stage: build

%post
    apt-get update && apt-get install -y git make build-essential libz-dev
    mkdir /opt/laynii2
    cd /opt/laynii2 
    git clone https://github.com/layerfMRI/laynii
    cd ./laynii
    make all
